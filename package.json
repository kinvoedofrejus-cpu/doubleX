{
  "name": "tontine-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute tontine-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute tontine-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "@noble/secp256k1": "^2.1.0",
    "@noble/hashes": "^1.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20240925.0"
  }
}
